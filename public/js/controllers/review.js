'use strict';

angular.module('myApp')
  .controller('ReviewCtrl', function ($scope, ReviewFactory, $routeParams) {
    // $scope.reviews = ReviewFactory.get({},function (res) {
    //   console.log($scope.reviews)
    // })
    // console.log($routeParams.slug)
    
  })